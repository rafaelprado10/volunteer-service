apiVersion: v1
kind: Service
metadata:
  name: volunteer-service
  namespace: volunteer-service
spec:
  selector:
    app: volunteer-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP